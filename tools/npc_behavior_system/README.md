# NPC behavior system (compatibility stub)

Этот файл оставлен для бесконфликтного merge/rebase веток, где ещё есть ссылки на `tools/npc_behavior_system/README.md`.

Актуальный active runtime-контур:
- `src/modules/npc/`

Архив legacy/reference-материалов:
- `docs/legacy/tools_reference/npc_behavior_system/`

Новые изменения в `tools/*` не вносятся.
