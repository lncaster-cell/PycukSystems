# Module 3 implementation backlog

## Perf-stage Definition of Done (DoD)

- [ ] **Обязательный шаг до merge:** automated fairness checks подключены и выполняются через `scripts/test_module3_fairness.sh` с обязательными флагами `--max-starvation-window`, `--enforce-pause-zero`, `--max-post-resume-drain-ticks`, `--min-resume-transitions`.
